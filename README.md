# website-to-pdf 

```sh
# Python virtual env [create & activate] 
python3 -m venv .website-to-pdf
source .website-to-pdf/bin/activate

# Install Dependency
pip install requests beautifulsoup4 weasyprint\n
#OR
pip install -r requirements.txt

python generator.py
```